{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-windows",
  "description": "Windows-only opener scopes for the managed-tier install path under Program Files. Kept in its own file because the backslash glob pattern fails to compile on non-Windows targets.",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        { "path": "C:\\Program Files\\ClaudeCode\\**" }
      ]
    }
  ]
}
