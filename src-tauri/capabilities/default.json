{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Cross-platform capability for the main window. Platform-specific opener scopes live in default-macos.json / default-linux.json / default-windows.json so that one OS's path globs (e.g. Windows backslashes) don't break the scope compile on the other OSes.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        { "path": "$HOME/.claude/**" },
        { "path": "**/.claude/settings.json" },
        { "path": "**/.claude/settings.local.json" }
      ]
    },
    "dialog:allow-open"
  ]
}
