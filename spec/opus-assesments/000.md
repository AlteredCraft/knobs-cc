---
run_date: 2026-04-27
§9.2: [correct] assesment of MCP transport types; remove "WebSocket" claim
§9.4: [correct] add "local" to scope list for MCP servers (https://code.claude.com/docs/en/mcp#push-messages-with-channels)
§10.4: [correct] restore autoMemoryEnabled to settings fields (https://code.claude.com/docs/en/memory#automemoryenabled)
§8: [not sure] review https://code.claude.com/docs/en/plugin-marketplaces#plugin-sources
§3_line_281: [wrong]  it is CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR https://code.claude.com/docs/en/tools-reference#bash-tool-behavior
§3_line_251: [correct]  DISABLE_FEEDBACK_COMMAND is the current name
§3_env_vars: [not sure]  I see these values in inventory.md and on the docs page: (https://code.claude.com/docs/en/env-vars)
§15_line_792: [incorrect] this is the correct "Start In" options list




---
 ▐▛███▜▌   Claude Code v2.1.119
▝▜█████▛▘  Opus 4.7 · Claude Max
  ▘▘ ▝▝    ~/Projects/knobs.cc

❯ I just had an agent verify the truths posted in spec/inventory.md, can you    
  validate                                                                      
  ⎿  ⧉ Selected 1 lines from spec/inventory.md in Visual Studio Code

  Read 1 file (ctrl+o to expand)

⏺ I'll validate the inventory's claims against live docs. Given the scope, I'll
  dispatch parallel research agents to cross-check different sections, then
  consolidate findings. The user-selected line 170 (allowedMcpServers) is in the
   managed-only section — I'll make sure that gets scrutinized too.

⏺ 5 agents finished (ctrl+o to expand)
   ├ Verify settings fields §2 · 6 tool uses · 34.1k tokens
   │ ⎿  Done
   ├ Verify env vars §3 · 5 tool uses · 37.6k tokens
   │ ⎿  Done
   ├ Verify hooks/skills/subagents §5-7 · 9 tool uses · 56.1k tokens
   │ ⎿  Done
   ├ Verify MCP/plugins/memory §8-10 · 11 tool uses · 52.5k tokens
   │ ⎿  Done
   └ Verify sandbox/keybindings/statusline/CLI §11-15 · 9 tool uses · 59.4k tokens
     ⎿  Done

⏺ Validation complete. The "canonical as of 2026-04-27" claim at line 15 is
  premature — the previous agent over-attested. Here's what holds up and what
  doesn't, organized by severity.

  Wrong claims (need correction)

  §9.2 line 568 — fabricated. "WebSocket confirmed as ws transport" is not in
  the MCP docs. Documented transports for claude mcp add are only stdio, sse,
  http. The "WebSocket" mention in docs is a passing reference in URL-allowlist
  context, not a transport type. Remove or downgrade.

  §9.4 line 589 — wrong scope list. --scope user|project is missing local (the
  default scope, where servers go in ~/.claude.json). Docs document three: local
   | project | user.

  §10.4 contradicts §2.9 — §10.4 is right. Docs explicitly document
  autoMemoryEnabled ("set autoMemoryEnabled in your project settings"). It was
  wrongly removed from §2 at line 191. Restore it as a settings field.

  §8 marketplace source types (lines 530-538) — multiple errors.
  - git row is wrong; live equivalent is url (a git URL).
  - url fields: should be url, ref?, sha? (not headers).
  - git-subdir missing entirely (fields: url, path, ref?, sha?).
  - github fields: ref?, sha? (not path).
  - npm missing version?, registry?.
  - file, directory, hostPattern, settings aren't documented as plugin source
  types — looks like a conflation of marketplace sources with plugin sources.

  §3 line 281 — typo. CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR should be
  CLAUDE_CODE_BASH_MAINTAIN_PROJECT_WORKING_DIR.

  §3 line 251 — stale name. DISABLE_BUG_COMMAND not in docs; the documented form
   is DISABLE_FEEDBACK_COMMAND.

  §3 — entries not on the env-vars page (drop, move with citation, or > 
  [!verify]): MCP_TIMEOUT, MCP_TOOL_TIMEOUT, ENABLE_CLAUDEAI_MCP_SERVERS,
  SLASH_COMMAND_TOOL_CHAR_BUDGET, CLAUDE_REMOTE_CONTROL_SESSION_NAME_PREFIX,
  OTEL_* (lives on monitoring page), AWS_REGION/CLOUD_ML_REGION (live on
  Bedrock/Vertex pages, not as named).

  §15 line 792 — incomplete mode list. --permission-mode missing default and
  dontAsk. (§4.1 has all six, so this is just the CLI row that's stale.)

  §2 line 113 — wrong tag. voiceEnabled is DEPRECATED in docs, not just "LEGACY"
   — belongs in §2.8 alongside includeCoAuthoredBy.

  §2 line 178 — wrong scope tag. disableAutoMode is in the general settings
  table, not managed-only.

  Stale/incomplete (not wrong, but the inventory says "canonical")

  §5.1 hook events — 9 listed + 4 confirmed
  (StopFailure/PostToolUseFailure/PermissionRequest/PermissionDenied), but ~15
  documented events are missing: InstructionsLoaded, UserPromptExpansion,
  PostToolBatch, SubagentStart, TaskCreated, TaskCompleted, TeammateIdle,
  ConfigChange, CwdChanged, FileChanged, WorktreeCreate, WorktreeRemove,
  PostCompact, Elicitation, ElicitationResult.

  §5.2 — agent handler is documented as experimental; not flagged.

  §7 subagents — missing disallowedTools and prompt (used by --agents JSON
  form).

  §3 env vars — ~70 documented vars missing. Notable: real OAuth env is
  CLAUDE_CODE_OAUTH_TOKEN (+ _REFRESH_TOKEN, _SCOPES);
  ANTHROPIC_SMALL_FAST_MODEL is DEPRECATED in docs (line 231 doesn't flag it);
  CLAUDE_CODE_USE_FOUNDRY, CLAUDE_CODE_USE_MANTLE, BASH_MAX_OUTPUT_LENGTH,
  CLAUDE_CODE_GLOB_TIMEOUT_SECONDS, CLAUDE_CODE_MAX_CONTEXT_TOKENS,
  CLAUDE_CODE_MAX_OUTPUT_TOKENS, etc.

  §12 statusline — missing top-level cwd; context_window.current_usage
  sub-fields not enumerated (input/output/cache_creation/cache_read); worktree
  sub-fields not enumerated (name/path/branch/original_cwd/original_branch);
  rate_limits.{five_hour,seven_day} each have used_percentage and resets_at.

  §14 JetBrains — Cmd+Esc/Ctrl+Esc "quick launch" shortcut not listed.

  §15 line 798 — --settings also accepts a JSON string, not just a file path.

  Confirmed correct

  - allowedMcpServers (line 171, your highlighted line) — managed-only ✓, shape
  [{serverName}] ✓, matches doc example.
  - All 20 keybinding contexts ✓.
  - Sandbox schema (every key) ✓.
  - All 5 hook handler types exist (with the experimental caveat on agent).
  - All 15 skill frontmatter fields exist verbatim.
  - All 11 listed subagent frontmatter fields exist.
  - forceLoginOrgUUID "empty array fails closed" — verbatim in doc.
  - §2.9 removed list (theme, preferredNotifChannel, etc.) — those are absent
  from docs, except autoMemoryEnabled which is documented and shouldn't have
  been removed.
  - §10 imports 5-level depth, memory precedence, rules paths frontmatter ✓.
  - §15 --setting-sources ✓.
  - VS Code shortcut claims ✓.
  - §16 deprecations ✓.

  Recommendation

  Roll back line 15's "declared canonical" claim until at minimum the wrong 
  claims above are fixed. The completeness gaps (especially §3 env vars and §5.1
   events) are larger than the previous agent let on — calling the inventory
  canonical is currently misleading.

  Want me to apply the fixes? I'd suggest two passes: (1) the corrections above
  with high confidence, then (2) a separate diff for the missing entries (env
  vars + hook events) so you can review additions in bulk.

✻ Baked for 4m 1s

※ recap: Validating spec/inventory.md against live Claude Code docs; the previous agent over-attested, so I surfaced wrong claims (e.g., MCP `ws` transport, marketplace source types, `autoMemoryEnabled` removal) and gaps. Next: apply the corrections on your go-ahead. (disable recaps in /config)